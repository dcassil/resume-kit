# Resume Kit ATS

Phase 3 deterministic ATS engine for resume evaluation. Provides ATS simulation and scoring services using deterministic rules and keyword-based matching without external LLM provider dependencies.
