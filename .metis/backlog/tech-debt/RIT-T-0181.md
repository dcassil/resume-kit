---
id: skill-doc-reconciliation-v0-15
level: task
title: "Skill-doc reconciliation: export gate, evidence-gate split, working-path, spine, determinism, naming"
short_code: "RIT-T-0181"
created_at: 2026-08-11T18:00:00+00:00
updated_at: 2026-08-11T18:00:00+00:00
parent: 
blocked_by: []
archived: false

tags:
  - "#task"
  - "#tech-debt"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: NULL
---

# Skill-doc reconciliation (v0.15 re-audit doc findings)

## Objective **[REQUIRED]**

Reconcile the skill/docs so they match the current code, closing the doc-only findings from the
code-verified v1.8.0 re-audit. No code behavior changes — documentation truth only.

## Backlog Item Details

### Type
- [x] Tech debt — documentation out of sync with shipped code (agent-misrouting / inaccuracy risk)

### Priority
- [x] P2 — Medium. None are code bugs, but several make skills describe behavior the code does not do
  (or omit behavior it does), which misleads orchestrating agents.

## Acceptance Criteria **[REQUIRED]**

Each sub-item is doc-only; verify against the cited code before editing so docs match reality.

- [ ] **E — export gate is documented.** `export-resume/SKILL.md` currently never mentions the enforced
      page budget or the override. Document that export renders, runs the page-budget check, and FAILS
      CLOSED over `max_pages` (no artifact), plus the `allow_over_length` override; add `root` and
      `allow_over_length` to its Inputs table (currently `export-resume/SKILL.md:45-49`). Ensure
      `finalize-resume` and `perfect` mention the override, not just the gate. (Code truth:
      `capabilities.py:1075-1090`, `models.py:756`, CLI `app.py:130-133`, MCP `tools.py:819`, API
      `routes.py:548,568-574`.)
- [ ] **B — evidence-gate split reconciled (doc).** `update-terminology` hard-requires
      `CandidateEvidence` while `seed-terminology`/`learn-terminology`/`check-keywords` grow aliases on
      human-confirmation only. Either document WHY (the distinct proof postures of a resume *edit* vs an
      alias *growth*) with cross-links, or align them. Currently unreconciled
      (`update-terminology:44-50` vs `seed-terminology:38-43`, `learn-terminology:119-166`,
      `check-keywords:92-101`). NOTE: the code-side alias-collision hardening is a SEPARATE task
      ([[RIT-T-0180]]); this item is the doc posture only.
- [ ] **D — working-path convention fixed.** The code and `_shared/apply-changes.md:30,35` use
      `working/<name>.tailored.json`; the review/export/tailor SKILL docs invented
      `working/<session>/resume.json` with NO code backing. Fix `review-resume/SKILL.md:27,35,48,163`,
      `export-resume/SKILL.md:18`, and `config-pointers.md:21,31` to the real path; drop the fictitious
      `<session>/resume.json`. (Code truth: `edit_session.py:978-986`.)
- [ ] **I — apply-changes vs update-structure contradiction removed.** `_shared/apply-changes.md:3-7`
      lists `update-structure` as an interactive-spine (ChangeProposal→runbook) caller, but
      `update-structure/SKILL.md:7,16,56-61` says it is auto-only (drives `build-base`, no per-fix
      interactive approval). Remove `update-structure` from the apply-changes caller list.
- [ ] **H — determinism labeling added.** The flow-cluster skills (`complete-resume-flow`,
      `tailor-resume`, `finalize-resume`) do not flag which finish steps are model-driven
      (`review-resume`, `interview-missing-job-description`) vs deterministic. Add a short
      deterministic-vs-agent-driven note to each flow.
- [ ] **J2 — rank-changes wording.** `rank-changes:80` still says "seed-only"; align to the
      `tailor-resume:39` wording ("seed lexicon only").
- [ ] **J5 — evidence capability naming mapping.** One capability has four surface names: skill dir
      `extract-evidence/`, CLI `build-evidence` (`app.py:584`), MCP `candidate_evidence_build`
      (`tools.py:88`), facade key `extract-evidence` (`capabilities.py:1563` → `build_candidate_evidence`).
      At minimum, document the mapping in the `extract-evidence` skill so users/agents aren't confused;
      renaming is out of scope here (would be a code task).
- [ ] Plugin markdown/slug tests remain green; no skill SLUGS added/removed.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
- Doc-only edits. For each sub-item, read the cited code first and make the doc match the code (not the
  reverse). Do NOT change code behavior.
- J3 from the audit is intentionally EXCLUDED — verified as by-design (alias provenance is optional and
  never affects matching), not a defect.

### Dependencies
- Reflects code shipped in [[RIT-I-0024]] (export gate, config-pointers) and the current edit-session
  working-path.
- The B doc item is the sibling of the [[RIT-T-0180]] code fix; keep the two consistent.

### Risk Considerations
- A wrong doc (e.g. citing a path/flag that does not exist) is worse than an omission — verify every
  path/flag/field against code before writing.

**Recommended Agent: sonnet + medium** — multi-file doc reconciliation grounded in cited code, plugin
markdown tests as the gate.

## Verification Steps **[REQUIRED]**

- [ ] `uv run --no-sync pytest plugins/resume-intelligence/tests/`
- [ ] Spot-grep that no doc cites `working/<session>/resume.json` or an export flag/field that does not
      exist in code.

## Status Updates **[REQUIRED]**

- 2026-08-11: Filed from the code-verified v1.8.0 re-audit (doc-only findings; J3 excluded as
  by-design; B code portion split to [[RIT-T-0180]]).
