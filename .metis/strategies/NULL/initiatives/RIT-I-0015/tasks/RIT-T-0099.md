---
id: implement-edit-session
level: task
title: "Implement edit-session orchestrator + hard write gate across facade/CLI/MCP/API"
short_code: "RIT-T-0099"
created_at: 2026-08-05T14:56:57.803146+00:00
updated_at: 2026-08-05T14:56:57.803146+00:00
parent: RIT-I-0015
blocked_by: [RIT-T-0098]
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0015
---

# Implement edit-session orchestrator + hard write gate across facade/CLI/MCP/API

## Parent Initiative

[[RIT-I-0015]]

## Objective **[REQUIRED]**

Implement the code-owned edit-session orchestrator and its hard write gate exactly as designed in
[[RIT-T-0098]], surfaced across facade + CLI + MCP + API with parity tests. After this task, applying an
edit to the working resume during improve is impossible without a logged accept/modify/reject decision,
and the three-mode loop (`interactive` / `review_at_end` / `auto`) is a real code flow rather than skill
prose.

## Context

The improve loop's human-in-the-loop UX is designed but not wired; a hard gate is required (per decision)
because a prose instruction was trivially bypassed in testing. This task lands the ADR design, composing
the truth semantics from [[RIT-T-0095]] and the feedback/evidence surfaces from [[RIT-T-0097]] over the
[[RIT-T-0091]] state contract.

## Acceptance Criteria **[REQUIRED]**

- [ ] `EditSession` schema + persistence implemented per [[RIT-T-0098]] (state under `resume-kit/`,
      atomic writes).
- [ ] Facade capabilities for the session lifecycle: `open-edit-session` (records mode), `present-change`
      (returns original vs proposed diff for a `change_id`), `decide-change` (accept / accept_modified /
      reject / undo + `reason_code`, appends `EditFeedback` via [[RIT-T-0097]]), `apply-change` (HARD
      GATE), `validate-session-truth`, `close-edit-session`.
- [ ] **Hard write gate enforced in code**: `apply-change` refuses any `change_id` without a logged
      decision, and refuses to persist to the working resume on truth failure; the refusal is a
      machine-readable error naming the offending change(s) and exits non-zero on CLI.
- [ ] All three modes implemented and behave per the ADR (`auto` applies only fully-supported,
      non-fabricating changes and defers the rest with a report).
- [ ] CLI + MCP + API expose the lifecycle; a stateless surface resumes the persisted session by id.
- [ ] Re-scoring integrates: after applies, the session can report updated `match`/`ats` deltas by calling
      existing scorers (no reimplementation).
- [ ] Unit tests: gate refuses unlogged write; gate refuses truth-failing accepted change; each mode's
      happy path. Cross-surface parity tests for the lifecycle operations.

## Test Cases **[CONDITIONAL: Testing Task]**

### Test Case 1: Unlogged write is refused
- **Test ID**: TC-001
- **Steps**: open a session; attempt `apply-change` for a `change_id` with no `decide-change` logged.
- **Expected Results**: refused with machine-readable error; working resume unchanged; non-zero CLI exit.
- **Status**: Pass/Fail

### Test Case 2: Truth-failing accepted change is refused
- **Test ID**: TC-002
- **Steps**: `decide-change` accept a change that introduces a `CONTRADICTED` claim; `apply-change`.
- **Expected Results**: apply refused on truth failure; reason surfaced.
- **Status**: Pass/Fail

### Test Case 3: auto mode defers judgment changes
- **Test ID**: TC-003
- **Steps**: run a session in `auto` with a mix of fully-supported and judgment-required changes.
- **Expected Results**: supported changes applied; judgment ones deferred and reported, not applied.
- **Status**: Pass/Fail

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
- Reuse `packages/alignment/apply.py` diff-application under the gate; the gate is a precondition check on
  logged decisions correlated by `change_id`, not a new apply engine.
- Compose `rank-edit-candidates` / `record-edit-feedback` / `validate-truth` from [[RIT-T-0097]]/[[RIT-T-0095]];
  do not duplicate their logic.

### Dependencies
Blocked by [[RIT-T-0098]] (design). Consumes [[RIT-T-0095]], [[RIT-T-0097]], [[RIT-T-0091]]. Blocks
[[RIT-T-0100]].

### Risk Considerations
High — most cross-surface code in the initiative; the stateless-surface session-resume path and atomic
state writes are the trickiest parts. Follow the ADR precisely; deviate only with a documented reason.

### Recommended Agent
**opus + high** — core cross-surface implementation with a load-bearing invariant (the hard gate).

## Status Updates **[REQUIRED]**

*To be added during implementation*
