---
id: add-attribution-rows-and-import-boundary-tests
level: task
title: "Add Attribution Rows and Import Boundary Tests"
short_code: "RIT-T-0025"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:30:00+00:00
parent: RIT-I-0004
blocked_by: [RIT-T-0018, RIT-T-0019, RIT-T-0020, RIT-T-0021, RIT-T-0022, RIT-T-0023]
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Add Attribution Rows and Import Boundary Tests

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

Close the compliance and architecture gates centrally: append provenance rows for all Phase 3 ported/adapted units and enforce no monolith/provider imports in the new packages.

## Acceptance Criteria

- [ ] `references/attribution.md` has concrete Phase 3 rows for matching keywords, matching predicates, ATS engine, job keyword extraction prompt/parser, match scoring if adapted, and before/after comparison if adapted, all pinned to SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.
- [ ] Each new package has an import-boundary test rejecting `app.*`, `litellm`, `openai`, and `anthropic` imports; ATS and matching tests also reject concrete provider imports.
- [ ] Boundary tests scan package source files and report file/line violations like the document-parser boundary test.
- [ ] The full gate passes, including existing 401 tests.

## Implementation Notes

Recommended Agent: sonnet + medium. Use the ledger format already established in `references/attribution.md`, adding a new `Ported Files — packages/matching, packages/ats, packages/job-parser` section or equivalent rows. Verify source markers are present in upstream-derived files before adding rows. Model boundary tests on `packages/document-parser/tests/test_import_boundaries.py`; do not introduce shared helper files unless necessary.

### Files this task may write
- `references/attribution.md`
- `packages/matching/tests/test_import_boundaries.py`
- `packages/ats/tests/test_import_boundaries.py`
- `packages/job-parser/tests/test_import_boundaries.py`

### Dependencies
- [[RIT-T-0018]] — Port Deterministic Keyword Matching
- [[RIT-T-0019]] — Port Matching Validation Predicates
- [[RIT-T-0020]] — Adapt Deterministic ATS Engine
- [[RIT-T-0021]] — Build Provider-Injected Job Description Parser
- [[RIT-T-0022]] — Implement Explainable Job-Match Scoring
- [[RIT-T-0023]] — Implement Resume Selection and Version Comparison

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run pytest packages/matching/tests/test_import_boundaries.py packages/ats/tests/test_import_boundaries.py packages/job-parser/tests/test_import_boundaries.py
3. uv run ruff check packages tests
4. uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser
5. uv run pytest
6. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser && uv run pytest

## Status Updates

*To be added during implementation*
