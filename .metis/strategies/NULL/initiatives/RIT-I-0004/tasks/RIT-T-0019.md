---
id: port-matching-validation-predicates
level: task
title: "Port Matching Validation Predicates"
short_code: "RIT-T-0019"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:41:21.559766+00:00
parent: RIT-I-0004
blocked_by: [RIT-T-0016]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Port Matching Validation Predicates

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

Bring forward only the reusable deterministic eval predicates needed in Phase 3: job keyword presence and resume schema validity.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `resume_kit_matching.predicates` exposes `jd_keywords_present` and `is_valid_resume` as pure deterministic helpers.
- [ ] Tests cover empty keywords, case-insensitive keyword presence, invalid resume payloads, and valid `ResumeDocument`/dict payloads.
- [ ] Fabrication, personal-info, and section-preservation evaluators are not ported in this phase.

## Implementation Notes

Recommended Agent: sonnet + medium. Port from `upstream/apps/backend/tests/evals/scorers.py`: `jd_keywords_present:152`, `is_valid_resume:161`, and only the helper text flattening needed by those functions. Replace `app.schemas.ResumeData` with `resume_kit_schemas.ResumeDocument`/`ResumeData`. Add the modified-source marker for the upstream scorers path and SHA. Attribution row is added by the final compliance task.

### Files this task may write
- `packages/matching/src/resume_kit_matching/predicates.py`
- `packages/matching/tests/test_predicates.py`

### Dependencies
- [[RIT-T-0016]] — Scaffold Phase 3 Workspace Packages

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run pytest packages/matching/tests/test_predicates.py
3. uv run mypy packages/matching
4. uv run ruff check packages/matching
5. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser && uv run pytest

## Status Updates

*To be added during implementation*