---
id: adapt-deterministic-ats-engine
level: task
title: "Adapt Deterministic ATS Engine"
short_code: "RIT-T-0020"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:41:21.609850+00:00
parent: RIT-I-0004
blocked_by: [RIT-T-0016]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Adapt Deterministic ATS Engine

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

Implement the ATS package by adapting the deterministic upstream ATS seed into an engine that produces canonical `ATSScore`/`ATSSubScores` and adds narrow deterministic recommendations.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `resume_kit_ats.engine` exposes `compute_ats_score` returning `resume_kit_schemas.ATSScore`.
- [ ] Seed behavior is characterization-tested against upstream weights: keyword_match 0.55, skills_coverage 0.25, section_completeness 0.20.
- [ ] Expanded deterministic checks add recommendation coverage for contact, section, date, and formatting risks without changing the seed composite contract unexpectedly.
- [ ] No LLM, no network, no `app.*`, and no import from `resume_kit_matching`.

## Implementation Notes

Recommended Agent: sonnet + medium. Adapt `upstream/apps/backend/app/services/ats.py`: `_extract_all_text:33`, `_keyword_in_text:51`, `_compute_skills_coverage:64`, `_compute_section_completeness:98`, `_generate_recommendations:129`, `compute_ats_score:171`. Invert dict return into `ATSScore`; accept canonical `ResumeDocument` and `JobDescription` while preserving dict compatibility where useful. Add the modified-source marker with upstream path and SHA. Keep package dependencies to core/schemas only.

### Files this task may write
- `packages/ats/src/resume_kit_ats/engine.py`
- `packages/ats/tests/test_engine_characterization.py`
- `packages/ats/tests/test_expanded_checks.py`

### Dependencies
- [[RIT-T-0016]] — Scaffold Phase 3 Workspace Packages

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run pytest packages/ats/tests/test_engine_characterization.py packages/ats/tests/test_expanded_checks.py
3. uv run mypy packages/ats
4. uv run ruff check packages/ats
5. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser && uv run pytest

## Status Updates

*To be added during implementation*