---
id: port-deterministic-keyword-matching
level: task
title: "Port Deterministic Keyword Matching"
short_code: "RIT-T-0018"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:30:00+00:00
parent: RIT-I-0004
blocked_by: [RIT-T-0016]
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Port Deterministic Keyword Matching

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

Implement the matching substrate from upstream refiner: whole-term keyword matching, keyword extraction from job keyword dictionaries, and gap analysis producing the existing `KeywordGapAnalysis` schema.

## Acceptance Criteria

- [ ] `resume_kit_matching.keywords` provides deterministic whole-term matching, resume text flattening, `calculate_keyword_match`, and `analyze_keyword_gaps`.
- [ ] Gap analysis returns `resume_kit_schemas.KeywordGapAnalysis` with missing, injectable, non-injectable, current match, and potential match fields.
- [ ] Characterization tests lock upstream behavior for whole-term boundaries, empty keyword sets returning 0%, injectable split, and custom-section text extraction.
- [ ] Source file carries the modified-source marker for `apps/backend/app/services/refiner.py` at SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.

## Implementation Notes

Recommended Agent: sonnet + medium. Port/adapt upstream `services/refiner.py:_keyword_in_text:38`, `_extract_jd_skill_keys:56`, `analyze_keyword_gaps:181`, `calculate_keyword_match:641`, and `_extract_all_text:682`. Invert `app.schemas.refinement` to `resume_kit_schemas.KeywordGapAnalysis`; accept `ResumeDocument | dict` and `JobDescription | dict` if that matches local style, but normalize through Pydantic/model dumps rather than ad hoc object inspection. Do not touch `references/attribution.md`; the compliance task appends the matching row centrally.

### Files this task may write
- `packages/matching/src/resume_kit_matching/keywords.py`
- `packages/matching/tests/test_keywords_characterization.py`

### Dependencies
- [[RIT-T-0016]] — Scaffold Phase 3 Workspace Packages

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run pytest packages/matching/tests/test_keywords_characterization.py
3. uv run mypy packages/matching
4. uv run ruff check packages/matching
5. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser && uv run pytest

## Status Updates

*To be added during implementation*
