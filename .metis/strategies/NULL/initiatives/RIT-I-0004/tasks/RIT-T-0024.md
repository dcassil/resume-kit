---
id: wire-public-exports-across-new
level: task
title: "Wire Public Exports Across New Packages"
short_code: "RIT-T-0024"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:41:21.810851+00:00
parent: RIT-I-0004
blocked_by: [RIT-T-0018, RIT-T-0019, RIT-T-0020, RIT-T-0021, RIT-T-0022, RIT-T-0023]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Wire Public Exports Across New Packages

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

After implementation modules exist, expose the stable Phase 3 public API through each package `__init__` without forcing parallel implementation tasks to share export files.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `resume_kit_matching` exports keyword matching, gap analysis, predicates, job-match, selection, and comparison entry points.
- [ ] `resume_kit_ats` exports `compute_ats_score` and any intentionally public ATS helper/result aliases.
- [ ] `resume_kit_job_parser` exports the job-description parse/extract entry points and prompt constant only if intended public.
- [ ] Import smoke tests pass for public symbols.

## Implementation Notes

Recommended Agent: haiku + low. Keep exports explicit via `__all__`, following `packages/document-parser/src/resume_kit_document_parser/__init__.py`. Avoid exporting private helpers such as `_keyword_in_text` unless tests require characterization access through a public wrapper.

### Files this task may write
- `packages/matching/src/resume_kit_matching/__init__.py`
- `packages/ats/src/resume_kit_ats/__init__.py`
- `packages/job-parser/src/resume_kit_job_parser/__init__.py`

### Dependencies
- [[RIT-T-0018]] — Port Deterministic Keyword Matching
- [[RIT-T-0019]] — Port Matching Validation Predicates
- [[RIT-T-0020]] — Adapt Deterministic ATS Engine
- [[RIT-T-0021]] — Build Provider-Injected Job Description Parser
- [[RIT-T-0022]] — Implement Explainable Job-Match Scoring
- [[RIT-T-0023]] — Implement Resume Selection and Version Comparison

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run python -c "from resume_kit_matching import check_job_match, select_best, compare_versions; from resume_kit_ats import compute_ats_score; from resume_kit_job_parser import parse_job_description"
3. uv run mypy packages/matching packages/ats packages/job-parser
4. uv run ruff check packages/matching packages/ats packages/job-parser
5. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser && uv run pytest

## Status Updates

*To be added during implementation*