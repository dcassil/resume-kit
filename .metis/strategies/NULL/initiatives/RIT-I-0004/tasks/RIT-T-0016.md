---
id: scaffold-phase-3-workspace-packages
level: task
title: "Scaffold Phase 3 Workspace Packages"
short_code: "RIT-T-0016"
created_at: 2026-08-04T01:30:00+00:00
updated_at: 2026-08-04T01:41:21.406209+00:00
parent: RIT-I-0004
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0004
---

# Scaffold Phase 3 Workspace Packages

## Parent Initiative

[[RIT-I-0004]] — Phase 3 — Matching & Deterministic Analysis

## Objective

Create the three new uv workspace packages first so later implementation tasks can work in disjoint package modules without competing over setup files.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `packages/matching`, `packages/ats`, and `packages/job-parser` each have a `pyproject.toml` mirroring the document-parser workspace convention.
- [ ] Each package has `src/<import_name>/__init__.py`, `src/<import_name>/py.typed`, `tests/__init__.py`, and a short README.
- [ ] Package dependencies point inward only to `resume-kit-core` and `resume-kit-schemas`; do not add `resume-kit-ats` to matching until the job-match task explicitly chooses that dependency.

## Implementation Notes

Recommended Agent: haiku + low. Mirror `packages/document-parser/pyproject.toml`: hatchling build backend, package names `resume-kit-matching`, `resume-kit-ats`, `resume-kit-job-parser`, import names `resume_kit_matching`, `resume_kit_ats`, `resume_kit_job_parser`, Python >=3.12, workspace sources for core/schemas. Keep `__init__.py` minimal for now; the export task will wire public APIs after implementations exist.

### Files this task may write
- `packages/matching/pyproject.toml`
- `packages/matching/README.md`
- `packages/matching/src/resume_kit_matching/__init__.py`
- `packages/matching/src/resume_kit_matching/py.typed`
- `packages/matching/tests/__init__.py`
- `packages/ats/pyproject.toml`
- `packages/ats/README.md`
- `packages/ats/src/resume_kit_ats/__init__.py`
- `packages/ats/src/resume_kit_ats/py.typed`
- `packages/ats/tests/__init__.py`
- `packages/job-parser/pyproject.toml`
- `packages/job-parser/README.md`
- `packages/job-parser/src/resume_kit_job_parser/__init__.py`
- `packages/job-parser/src/resume_kit_job_parser/py.typed`
- `packages/job-parser/tests/__init__.py`

### Dependencies
None

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and `resume_kit_ats` only if noted) ONLY; never import
`app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this task IS
the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv sync --all-packages
2. uv run python -c "import resume_kit_matching, resume_kit_ats, resume_kit_job_parser"
3. uv run ruff check packages tests
4. uv run mypy packages/core packages/schemas packages/document-parser packages/matching packages/ats packages/job-parser
5. uv run pytest

## Status Updates

*To be added during implementation*