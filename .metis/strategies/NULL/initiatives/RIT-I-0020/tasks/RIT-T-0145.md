---
id: phase-4-rename-standard-refine
level: task
title: "Phase 4 — Rename standard→refine + back-compat + config lineage + input resolution"
short_code: "RIT-T-0145"
created_at: 2026-08-07T18:35:54.439222+00:00
updated_at: 2026-08-08T00:00:00.000000+00:00
parent: RIT-I-0020
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: true
strategy_id: NULL
initiative_id: RIT-I-0020
---

# Phase 4 — Rename standard→refine core + back-compat + config lineage

## Parent Initiative
[[RIT-I-0020]] — REQ-001, REQ-008, NFR-003. Was blocked on [[RIT-I-0019]] (structure substrate);
resumed after both [[RIT-I-0019]] and [[RIT-I-0021]] landed on main.

## Objective
Rename the wording pass and lineage `standard → refine` in the core (config + facade + scoring) with
FULL backward compatibility, and finalize input resolution `structure ?? base ?? original`.

## Acceptance Criteria (all met)
- [x] `build_standard → build_refine`; writes `<name>-refine.json`; `build_standard`/`BuildStandardResult`
      retained as aliases (`BuildRefineResult` with `.refine_path`; `.standard_path` property alias).
- [x] `ProjectConfig` gains `refine_resume`/`refine_derived_from`; legacy `standard_*` kept as read-alias.
- [x] `resolve_active_resume`: `refine → standard(legacy) → structure → base → original`.
- [x] `set_version` accepts `refine=`/`refine_derived_from=`; legacy-only project resolves + forward-migrates.
- [x] Input resolution `structure ?? base ?? original` (canonical→BuildDoc projection when structure).
- [x] `StandardFixResult → RefineFixResult` alias; scoring exports both. Back-compat + resolution tests.

## Execution profile
opus + high.

## Status Updates
- DONE. Codex implemented core rename with aliases; orchestrator updated lineage/e2e artifact-name
  assertions (standard→refine), gated green, committed `fb4dd32`. Merged in `4c063ab`.
