---
id: phase-5-surfaces-skill-workflow
level: task
title: "Phase 5 — Surfaces + skill + workflow + deprecation aliases (refine lexicon)"
short_code: "RIT-T-0146"
created_at: 2026-08-07T18:35:55.927611+00:00
updated_at: 2026-08-08T00:00:00.000000+00:00
parent: RIT-I-0020
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: true
strategy_id: NULL
initiative_id: RIT-I-0020
---

# Phase 5 — Surfaces + skill + workflow + deprecation aliases

## Parent Initiative
[[RIT-I-0020]] — REQ-009, NFR-004.

## Objective
Expose `build-refine` across facade/CLI/MCP/API with parity, keeping every `build-standard` surface
as a one-release deprecation alias; rename the skill `update-best-practices → update-refine` (old dir
kept as a stub); update `resume-workflow` to name the pass `refine` between `structure` and `tailored`.

## Acceptance Criteria (all met)
- [x] `build-refine` facade capability + REGISTRY + CLI command + MCP tool + API route added.
- [x] `build-standard` surfaces retained as deprecation aliases (unchanged `standard_path` response schema).
- [x] Cross-surface parity case for `build-refine` added; existing standard parity case kept.
- [x] Skill `update-refine` created; `update-best-practices` replaced with a deprecation stub.
- [x] `resume-workflow` reconciled to the refine lexicon.
- [x] `EXPECTED_SKILL_SLUGS` updated (`update-refine` + the pre-existing `perfect`, fixing a prior
      skill-registry test gap left by [[RIT-I-0021]]); `update-best-practices` stub kept.
- [x] Job-hunter bridge: no `build-standard` dispatch existed, so no change needed.

## Execution profile
opus + medium. Run as two file-disjoint parallel codex agents (packages surfaces ‖ plugins skills).

## Status Updates
- DONE. Two parallel codex agents; orchestrator reverted a premature version bump, gated green,
  committed `1db1363`. Merged in `4c063ab`.
