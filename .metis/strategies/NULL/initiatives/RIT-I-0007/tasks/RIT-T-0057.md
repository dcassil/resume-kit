---
id: add-the-plugin-export-skill
level: task
title: "Add the plugin export skill"
short_code: "RIT-T-0057"
created_at: 2026-08-04T04:10:00+00:00
updated_at: 2026-08-04T15:59:27.350634+00:00
parent: RIT-I-0007
blocked_by: [RIT-T-0052]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0007
---

# Add the plugin export skill

## Parent Initiative

[[RIT-I-0007]] — Phase 6 — Export & Packaging

## Objective

Add the plugin skill documentation for the new `export-resume` capability under `plugins/resume-intelligence/skills/export-resume/SKILL.md`, matching the style of existing skills such as `plugins/resume-intelligence/skills/check-resume-ats/SKILL.md`. The skill must describe the facade capability, CLI command, MCP tool, API route, input schema, output envelope, and deterministic constraints from RIT-I-0007.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `plugins/resume-intelligence/skills/export-resume/SKILL.md` exists with frontmatter `name: export-resume` and an accurate description.
- [ ] The skill documents inputs `resume: ResumeDocument`, `format: pdf|docx`, optional export options, and output `InterfaceResponse` with `artifacts: list[ArtifactRef]` plus transport-specific byte/base64 behavior.
- [ ] The skill explicitly states the capability is deterministic, no-LLM, no frontend, no Chromium, and uses ReportLab/python-docx only inside `packages/export`.
- [ ] The skill names the thin adapters: CLI `resume-tool export`, MCP `resume_export`, API `POST /export`, and bridge `export_resume`.

## Implementation Notes

Keep this as documentation/instructions only. Do not modify existing skill files or add plugin runtime code. Avoid claiming Phase 7 features such as cover letters, audit, or deferred engine capabilities exist.

### Files this task may write
- `plugins/resume-intelligence/skills/export-resume/SKILL.md`

### Dependencies
- [[RIT-T-0052]] — Add the export-resume facade capability

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and sibling Phase-4 packages only if noted) ONLY; never
import `app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this
task IS the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv run ruff check plugins || true
2. test -f plugins/resume-intelligence/skills/export-resume/SKILL.md

## Status Updates

*To be added during implementation*