---
id: retire-ats-nonstandard-section
level: task
title: "Retire redundant ATS NONSTANDARD_SECTION finding (sequenced last)"
short_code: "RIT-T-0140"
created_at: 2026-08-07T18:27:20.000000+00:00
updated_at: 2026-08-07T18:27:20.000000+00:00
parent: RIT-I-0019
blocked_by: [RIT-T-0139]
archived: false

tags:
  - "#task"
  - "#phase/active"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0019
---

# Retire redundant ATS NONSTANDARD_SECTION finding

## Parent Initiative **[CONDITIONAL: Assigned Task]**

[[RIT-I-0019]]

## Objective **[REQUIRED]**

Phase 8 (sequenced last) of RIT-I-0019. Now that the shape analyzer (RIT-T-0135) owns section
classification, remove/neutralize the redundant `NONSTANDARD_SECTION` emission in
`resume_kit_ats/engine.py` so `base` and `structure` do not double-report the same condition.
This must not change any `base` gate or the `base` report's output shape otherwise.

## Acceptance Criteria **[REQUIRED]**

- [ ] The `NONSTANDARD_SECTION` finding is removed or neutralized in
      `packages/ats/src/resume_kit_ats/engine.py` so it is no longer emitted by the ATS
      structural check.
- [ ] `base`'s report shape and all `base` gates are otherwise **unchanged** — no other finding
      is added, dropped, or altered; exit codes and gate behavior are identical.
- [ ] Any test asserting `NONSTANDARD_SECTION` is updated to reflect that shape analysis now owns
      section classification (point it at the shape analyzer or remove the stale assertion),
      without weakening coverage of the remaining ATS findings.
- [ ] The RIT-T-0139 E2E and the full base/standard suites remain green after the change.
- [ ] `_CONVENTIONAL_SECTION_KEYWORDS` handling stays consistent with the RIT-T-0134 policy
      seed (do not reintroduce a divergent copy).

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
Locate the `NONSTANDARD_SECTION` emission in `engine.py` and remove it (or guard it off) while
leaving every other structural finding intact. Search the test suite for references and update
them. Keep the change surgical — this is a cleanup, not a refactor of the ATS engine. Confirm no
downstream surface relied on the finding.

### Dependencies
- **Blocked by [[RIT-T-0139]]** (sequenced last per the initiative so no detection gap exists
  before shape analysis is fully wired + tested).

### File claims (exclusive)
- `packages/ats/src/resume_kit_ats/engine.py`
- affected `packages/ats/tests/**` (and any other test asserting the finding)

### Risk Considerations
- **Detection gap** — mitigated by sequencing last, after the shape analyzer covers section
  classification.
- **Accidental base-shape change** — assert `base` report shape/gates unchanged via the existing
  base tests.

## Execution Profile

Recommended Agent: sonnet + medium

## Status Updates **[REQUIRED]**

### 2026-08-07 — In progress

Dispatched to codex (Wave 8): retire redundant ATS NONSTANDARD_SECTION now that shape analysis owns section classification.

## Verification

- `uv run --no-sync pytest packages/ats packages/facade packages/scoring integrations` green.
- `uv run --no-sync mypy packages/ats` clean; `ruff` clean.
