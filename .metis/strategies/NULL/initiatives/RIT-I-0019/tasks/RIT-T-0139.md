---
id: e2e-structure-test-docs-bump
level: task
title: "E2E original→base→structure→standard integration test + docs reconcile + version bump"
short_code: "RIT-T-0139"
created_at: 2026-08-07T18:27:20.000000+00:00
updated_at: 2026-08-07T18:27:20.000000+00:00
parent: RIT-I-0019
blocked_by: [RIT-T-0138]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: true
strategy_id: NULL
initiative_id: RIT-I-0019
---

# E2E integration test + docs reconcile + version bump

## Parent Initiative **[CONDITIONAL: Assigned Task]**

[[RIT-I-0019]]

## Objective **[REQUIRED]**

Phase 7 of RIT-I-0019. Add a real-fixture end-to-end test proving the full lineage
`original → base → structure → standard`, reconcile README/docs, and bump versions.

## Acceptance Criteria **[REQUIRED]**

- [ ] An integration test (under the facade/scoring integration test tree) drives a real
      multi-custom-section fixture (`resume-a` or equivalent) through ingest → `base` →
      `build_structure` → `build_standard` and asserts:
      - `structure` conforms to the canonical `Resume` schema;
      - `structure` is **content-lossless** vs `base` (the `ContentLedger` is fully accounted —
        no unaccounted substantive token);
      - the redundant "Core Skills" / "Technical Skills" / "Domains & Industries" case is
        merged/mapped as expected (canonical `skills` deduped; ambiguous sections deferred, not
        dropped);
      - `build_standard` runs **unchanged** on the projected structure and still writes
        `standard`;
      - config lineage pointers (`structure_resume`, `structure_derived_from`) are set correctly.
- [ ] The fixture is committed under the scoring/facade test trees.
- [ ] No existing `base`/`standard` test regresses (full suite green).
- [ ] README / relevant docs reconciled to describe the new `structure` stage and the
      `original → base → structure → standard` lineage.
- [ ] Version bump for the affected package(s) and plugin, consistent with the repo's
      independent-track versioning (record the chosen versions in the status update). No PyPI
      publish in this task — release is a separate orchestrator decision.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
Model the test on the existing baselining E2E test (RIT-T-0121) and the ScoreDoc E2E
(RIT-T-0111). Use a committed fixture with the redundant/non-standard sections described in the
initiative's Use Case 1. Assert artifacts, ledger accounting, lineage pointers, and
standardize-unchanged parity. Update README/docs prose only (no behavior change). Bump versions
in the relevant `pyproject.toml`(s) and plugin manifest.

### Dependencies
- **Blocked by [[RIT-T-0138]]** (surfaces + skill complete).

### File claims (exclusive)
- integration test module(s) + committed fixture under `packages/*/tests` or `integrations/`
- `README.md` + affected docs
- `pyproject.toml` version fields + plugin manifest version

### Risk Considerations
- **Flaky/over-specific assertions** — assert structural invariants (schema conformance, ledger
  fully accounted, standard produced) rather than brittle exact strings where avoidable.

## Execution Profile

Recommended Agent: opus + medium

## Status Updates **[REQUIRED]**

### 2026-08-07 — Completed

Landed packages/facade/tests/test_structure_lineage_e2e.py + fixture: original→base→structure→standard, canonical schema validation, ledger fully accounted, redundant Core/Technical Skills deduped, Domains & Industries deferred (not coerced), standard produced from projected structure, lineage pointers asserted. README reconciled. Version bump (orchestrator): resume-kit 0.9.0→0.10.0, plugin 1.2.0→1.3.0, marketplace 0.4.0→0.5.0. FULL suite green (pytest exit 0).

## Verification

- Full gate green: `ruff` clean, `mypy` clean, `uv run --no-sync pytest packages integrations
  plugins tests` all pass including the new E2E test.

## Note on ordering
The ATS `NONSTANDARD_SECTION` retirement (RIT-T-0140) is sequenced AFTER this task per the
initiative; if 0140's change affects `base` report shape, re-run this E2E after 0140 lands.
