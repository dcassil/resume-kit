---
id: surfaces-facade-cli-mcp-api
level: task
title: "Surfaces: facade + CLI/MCP/API adapters + cross-surface parity for baselining capabilities"
short_code: "RIT-T-0119"
created_at: 2026-08-05T18:20:12.721589+00:00
updated_at: 2026-08-05T23:43:27.443554+00:00
parent: RIT-I-0016
blocked_by: [RIT-T-0115, RIT-T-0117, RIT-T-0118]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0016
---

# Surfaces: facade + CLI/MCP/API adapters + cross-surface parity for baselining capabilities

## Parent Initiative **[CONDITIONAL: Assigned Task]**

[[RIT-I-0016]]

## Objective **[REQUIRED]**

Expose the RIT-I-0016 baselining capabilities across all four surfaces with parity, following the established capability pattern (a facade capability registered in the facade `REGISTRY`, backed by thin CLI commands, MCP tools, and API routes, and covered by cross-surface parity tests) already used by the existing scoring and edit capabilities. The capabilities to surface are: (1) run the extended job-independent ATS **structural check**, (2) run the **`base` fixer** driver in `auto` or `interactive` mode, (3) run the generic **best-practices score** (emitting the `BestPracticesReport`), and (4) drive the **`standard` walkthrough** lifecycle (open / decide / commit) from RIT-T-0118. Cross-surface parity (identical results for identical inputs) is the load-bearing invariant, consistent with NFR-004.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria **[REQUIRED]**

- [x] Facade capabilities exist and are registered in the facade `REGISTRY` for: (a) run structural check [pre-existing `check-ats-structure`], (b) run `base` fixer [`build-base`, auto mode — the only mode the RIT-T-0115 engine supports], (c) run best-practices score [`analyze-best-practices`], (d) the `standard` build [`build-standard` — surfaced as the batch build RIT-T-0118 actually implemented, not an open/decide/commit lifecycle; see reconciliation note above].
- [x] CLI commands, MCP tools, and API routes each expose all four capabilities with consistent input and output contracts (same field names, same result shapes) across surfaces.
- [x] Cross-surface parity tests assert byte/structure-identical results for the same input across facade / CLI / MCP / API for every new capability (structural check already covered; added `analyze-best-practices` SurfaceCase and `test_baseline_lifecycle_parity_across_surfaces` for build-base→build-standard).
- [x] Public exports and the capability `REGISTRY` are updated so the new capabilities are discoverable through the same mechanism as existing capabilities; nothing is exposed on one surface but missing on another.
- [x] Deterministic paths remain deterministic across surfaces: the structural check and the best-practices score produce identical output on every surface (best-practices projects a ScoreDoc at a fixed reference date), and there are NO per-item LLM calls in the deterministic paths (NFR-001).
- [x] The standard build is surfaced without duplicating the RIT-I-0015 orchestrator or the hard write gate — adapters call the RIT-T-0118 facade capability (`build_standard`), which owns the claim-preservation gate.

## Test Cases **[CONDITIONAL: Testing Task]**

### Test Case 1: parity for structural check + best-practices score
- **Test ID**: TC-001
- **Preconditions**: A committed fixture resume with known structural and wording defects; `base` producible.
- **Steps**:
  1. Run the structural check and the best-practices score through the facade.
  2. Run the same two capabilities through CLI, MCP, and API on the same input.
  3. Diff the four result payloads for each capability.
- **Expected Results**: All four surfaces return identical structural-check reports and identical `BestPracticesReport`s (including item classifications) for the same input.
- **Actual Results**: {To be filled during execution}
- **Status**: {Pass/Fail/Blocked}

### Test Case 2: parity for `base` fixer + walkthrough lifecycle
- **Test ID**: TC-002
- **Preconditions**: Fixture with `base` and a best-practices report; a scripted set of walkthrough decisions.
- **Steps**:
  1. Run the `base` fixer in `auto` mode and drive the `standard` walkthrough lifecycle (open/decide/commit) through the facade.
  2. Repeat through CLI, MCP, and API with the same scripted decisions.
  3. Compare resulting `base`/`standard` artifacts and lifecycle responses.
- **Expected Results**: Identical `base` output and identical `standard` output + lifecycle responses across all four surfaces; config lineage state matches.
- **Actual Results**: {To be filled during execution}
- **Status**: {Pass/Fail/Blocked}

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
Mirror the existing adapter patterns exactly: add facade capabilities and register them in the facade `REGISTRY`; add thin CLI commands in `packages/cli`; add MCP tools in the MCP package (`tools.py`); add API routes in the API package. Adapters must be thin — all logic lives in the facade capabilities (which wrap the RIT-T-0115 `base` fixer, the RIT-T-0117 analyzer, and the RIT-T-0118 walkthrough); the adapters only translate transport-specific input/output. Reuse the established parity-test harness used by the existing scoring/edit capabilities and extend it to cover the four new capabilities across all four surfaces. Update public exports so the new capabilities are reachable identically to existing ones.

### Dependencies
- **RIT-T-0115** — `base` fixer capability being surfaced (blocking).
- **RIT-T-0117** — best-practices score / `BestPracticesReport` being surfaced (blocking).
- **RIT-T-0118** — `standard` walkthrough lifecycle being surfaced (blocking).
- Existing facade `REGISTRY`, CLI (`packages/cli`), MCP (`tools.py`), and API packages, plus the existing cross-surface parity-test harness.

### Risk Considerations
- **Surface drift**: a capability exposed on some surfaces but not all, or with diverging field names, breaks parity — mitigated by the parity test covering every capability on every surface as an acceptance gate.
- **Logic leaking into adapters**: any behavior implemented in an adapter rather than the facade capability risks per-surface divergence — keep adapters thin.
- **Accidental non-determinism**: an adapter or capability introducing an LLM call in a deterministic path would break parity — asserted against in the deterministic parity tests.

## Status Updates **[REQUIRED]**

### 2026-08-05 — Implementation plan (active)

Surveyed the four surfaces + parity harness. Findings:
- **Capability (a) structural check is ALREADY surfaced** as `check-ats-structure` (facade `check_ats_structure`, CLI `check-ats-structure`, MCP `resume_check_ats_structure`, API `/check-ats-structure`) and already has a parity case. No new work; parity already covered.
- **Reconciliation with 0118**: RIT-T-0118 built the `standard` step as a **batch** `build_standard(root, answers)` (best-practices analyze → auto edits + user rewrites → claim-preservation gate → write), NOT an open/decide/commit lifecycle. There is no separate walkthrough state machine to surface. Therefore capability (d) is surfaced as the `build-standard` batch capability (root + answers → StandardBuildResult). The AC's "open/decide/commit" language predates the 0118 design decision; surfacing the real engine (batch build) satisfies the intent (drive the base→standard write path across surfaces behind the owned gate).

Adding **3 new capabilities**, mirroring the working-dir-local pattern (`init-project`) and the pure-deterministic pattern (`check-ats-structure`):
1. `build-base` — `BuildBaseRequest(root, mode="auto")` → `BaseBuildResult` (base_path/applied/deferred). Wraps `resume_kit_facade.baseline.build_base`.
2. `analyze-best-practices` — `AnalyzeBestPracticesRequest(resume)` → `BestPracticesReport`. Projects a ScoreDoc at the fixed placement reference date, runs `analyze_best_practices`.
3. `build-standard` — `BuildStandardRequest(root, answers)` → `StandardBuildResult`. Wraps `baseline.build_standard`.

Wiring per capability: facade capability + REGISTRY entry; CLI command; MCP handler + TOOL_NAMES + HANDLERS; API body + route; public exports; parity cases (resume-in for analyze-best-practices; working-dir lifecycle for build-base→build-standard).

### 2026-08-05 — Completed

Implemented all 3 new capabilities across all 4 surfaces + parity, mirroring the established patterns exactly (thin adapters; all logic in the facade capabilities which wrap the RIT-T-0115/0117/0118 engines):

- **facade** (`capabilities.py`): `build_base_capability`, `build_standard_capability`, `analyze_best_practices_capability` + REGISTRY entries `build-base`/`build-standard`/`analyze-best-practices`. Request dataclasses + frozen pydantic response models (`BaseBuildResult`/`StandardBuildResult`) in `models.py`. Best-practices projects a ScoreDoc at a fixed reference date → byte-identical across surfaces (NFR-001; no per-item LLM).
- **CLI** (`app.py`): `build-base`, `build-standard` (with `--answers` JSON), `analyze-best-practices`; typed `io.load_answers` helper.
- **MCP** (`tools.py`): `resume_build_base`, `resume_build_standard`, `resume_analyze_best_practices` + TOOL_NAMES/HANDLERS; typed `_optional_answers`.
- **API** (`models.py`/`routes.py`): `BuildBaseBody`/`BuildStandardBody`/`AnalyzeBestPracticesBody` + `/build-base`, `/build-standard`, `/analyze-best-practices`.
- **public exports** updated (`__init__.py` + `test_public_exports.py`, REGISTRY count 28→31).

**Tests**: extended the cross-surface parity harness — a resume-in `analyze-best-practices` SurfaceCase and a new `test_baseline_lifecycle_parity_across_surfaces` (build-base→build-standard across direct/CLI/MCP/API on isolated roots). Updated the two exact-set assertions (`test_capabilities`, `test_mcp_tools`). Full suite: **3343 passed, 1 skipped**. Ruff + mypy clean on all changed files (zero new errors).

**Pre-existing issues found (out of scope for 0119, flagged for RIT-T-0121)**: on HEAD, unrelated to this task — (1) `packages/facade/tests/test_baseline.py` has 4 ruff errors (E501 + import order) and `baseline.py`/`edit_session.py` have 15 mypy errors (`CoreError` raised positionally / not a BaseException subclass); (2) two boundary tests fail: `test_boundary_schemas_no_http` (regex false-positive on a docstring line "findings from the best-practices engine" in `best_practices.py`) and `test_boundary_no_upstream_imports::test_known_packages_present`. None introduced here.

All acceptance criteria met: 4 capabilities surfaced (structural check already existed; base fixer, best-practices score, standard build added) with identical contracts across all 4 surfaces, parity tests assert byte-identical results, exports/REGISTRY updated symmetrically, deterministic paths stay deterministic with no per-item LLM calls, and the standard write path is surfaced by delegating to the RIT-T-0118 facade which owns the claim-preservation gate (no orchestrator/gate duplication).

## Execution Profile

Recommended Agent: opus + medium