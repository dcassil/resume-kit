---
id: integration-remove-deprecated
level: task
title: "Integration: remove deprecated skills, reconcile tests + README, bump version"
short_code: "RIT-T-0081"
created_at: 2026-08-05T01:24:28.841958+00:00
updated_at: 2026-08-05T01:47:48.906250+00:00
parent: RIT-I-0011
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0011
---

# Integration: remove deprecated skills, reconcile tests + README, bump version

## Parent Initiative **[CONDITIONAL: Assigned Task]**

[[RIT-I-0011]]

## Objective **[REQUIRED]**

The cross-cutting integration pass (orchestrator-run) that reconciles the shared files everything touches,
removes the deprecated skills, and brings the whole plugin green. Kept as ONE task to avoid concurrent
edits to the shared test + README.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria **[REQUIRED]**

- [ ] Remove the deprecated skill directories: `extract-resume`, `extract-job-description`, `align-resume`,
      `check-resume-ats`, `check-resume-job-match`, and the old `align-terminology` (superseded by
      `update-terminology`). Underlying MCP tools / CLI / facade capabilities remain untouched.
- [ ] Reconcile `plugins/resume-intelligence/tests/test_skill_markdown.py`: `EXPECTED_SKILL_SLUGS` equals
      the final set — `resume-to-json`, `job-to-json`, `check-ats-structure`, `check-keyword-match`,
      `identify-resume-gaps`, `inject-keywords`, `update-terminology`, `validate-resume-truth`,
      `build-candidate-evidence`, `compare-resume-versions`, `select-best-resume`, `export-resume`,
      `manage-synonyms`, `resume-workflow`. Update `EXPECTED_CLI_OR_MCP` (map `check-ats-structure` →
      the RIT-T-0077 CLI/MCP tokens, `check-keyword-match` → match/`resume_check_job_match`; treat
      `inject-keywords`, `update-terminology`, `resume-workflow` as workflow skills exempt from the
      resume-tool/MCP mapping, like `manage-synonyms`). If a `_shared/prerequisites.md` reference exists,
      ensure the slug-discovery test ignores non-skill dirs (adjust discovery, not by loosening intent).
- [ ] Rewrite the README skill/tool table to the final set with a clear "provider required?" column and a
      short **Workflow** section pointing at `resume-workflow`; remove rows for deprecated skills; add a
      note that LLM auto-rewrite (`align-resume`) is disabled and the underlying `resume_extract` /
      `job_description_extract` LLM tools remain callable but are not surfaced as skills (prefer
      `resume-to-json` / `job-to-json`).
- [ ] Fix any lingering cross-links to removed/renamed skills across all remaining SKILL.md files
      (e.g. references to `align-terminology`, `identify-resume-gaps` phrasing, `check-resume-*`).
- [ ] Bump the plugin version in `plugins/resume-intelligence/.claude-plugin/plugin.json`.
- [ ] Gate: `claude plugin validate plugins/resume-intelligence` passes; `uv run --no-sync pytest plugins
      tests` green (slug set complete, frontmatter, resume-tool/MCP mapping); full `uv run --no-sync pytest
      packages integrations plugins tests` green; `ruff` + `mypy --strict` clean.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Recommended Agent
`sonnet + medium` — Mechanical but cross-cutting reconciliation (removals + expected-set + README +
version) requiring care to match the exact final slug set and the test's mapping rules. Orchestrator
typically runs this directly. Judgment needed on the CLI/MCP mapping + README, so medium not low.

### Technical Approach
- Run LAST, after 0077/0078/0079/0080 have landed, so the actual skill dirs match the target set before
  reconciling the slug test.
- `git rm -r` the deprecated skill dirs; grep the plugin tree for their names and fix references.
- Read `test_skill_markdown.py` (`EXPECTED_SKILL_SLUGS`, `EXPECTED_CLI_OR_MCP`, the workflow-skill
  exemption used for `manage-synonyms`/`align-terminology`) and mirror that pattern for the new workflow
  skills.

### Dependencies
- Depends on: RIT-T-0077, 0078, 0079, 0080 (all skills exist in final form). This is the initiative exit.

### Risk Considerations
- **Slug test mismatch**: EXPECTED set must exactly equal the directory set (minus non-skill dirs like
  `_shared`) — adjust discovery to ignore `_shared`, don't weaken the test.
- **Dangling cross-links**: grep-verify no SKILL.md references a removed slug.
- **Version/validate**: bump version; run `claude plugin validate` + full gate.

### Verification Steps
1. `git rm` deprecated dirs; `grep -rn` old slugs across plugins → none remain.
2. `claude plugin validate plugins/resume-intelligence` passes.
3. `uv sync --all-packages --reinstall-package resume-kit` then `uv run --no-sync pytest packages integrations plugins tests`; `ruff check` + `mypy` clean.

## Status Updates **[REQUIRED]**

COMPLETED (2026-08-04, orchestrator). Removed 6 deprecated skill dirs (extract-resume, extract-job-description, align-resume, check-resume-ats, check-resume-job-match, align-terminology); reconciled EXPECTED_SKILL_SLUGS (final 14) + EXPECTED_CLI_OR_MCP (check-ats-structure→resume_check_ats_structure, check-keyword-match→resume_check_job_match; workflow/agent-driven skills exempt); slug-discovery now ignores `_`-prefixed dirs (_shared); rewrote README capability map + added Workflow section + disabled/not-surfaced note; fixed dangling cross-links across 8 skills (via cleanup subagent — alias-context→check-keyword-match, generic→both; kept CLI tokens + the "no align-resume" note); bumped plugin 0.2.0→0.3.0. Final gate: ruff+mypy clean (80 src), plugin validate OK, plugin/skill tests green, 2715 passed/1 skipped.