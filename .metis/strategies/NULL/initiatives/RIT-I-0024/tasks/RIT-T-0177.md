---
id: standard-refine-doc-cleanup
level: task
title: "T3: standard->refine doc cleanup + deprecated-route removal"
short_code: "RIT-T-0177"
created_at: 2026-08-11T15:00:00+00:00
updated_at: 2026-08-11T15:00:00+00:00
parent: RIT-I-0024
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0024
---

# T3: standard->refine doc cleanup + deprecated-route removal

## Parent Initiative **[REQUIRED]**

[[RIT-I-0024]]

## Objective **[REQUIRED]**

Finish the `standard→refine` rename in the single-purpose skill docs. The rename is applied at the new
flow layer, but several skills still describe `original → base → standard` and route deferred work to
the deprecated `update-best-practices`, which misroutes agents.

## Acceptance Criteria **[REQUIRED]**

- [ ] `update-shape/SKILL.md`, `update-structure/SKILL.md`, `check-ats-view/SKILL.md`, and
      `check-structure/SKILL.md` describe the lineage as `original → base → structure → refine` (no
      stale `standard`).
- [ ] Active-flow routing points at `update-refine`, not the deprecated `update-best-practices`.
- [ ] The deprecated `update-best-practices` alias skill MAY remain for back-compat, but is not the
      routed target in any active flow doc.
- [ ] A repo-wide grep for `-> standard` / `→ standard` / `update-best-practices` in active skill docs
      returns only intentional back-compat mentions (documented as deprecated).
- [ ] Plugin markdown/slug tests remain green (no slug set changes expected).

## Implementation Notes **[REQUIRED]**

### Technical Approach
- Mechanical, doc-only edits. Match the vocabulary already used by `update-refine/SKILL.md` and the new
  flow skills.
- Grep first to enumerate every stale occurrence, then edit; re-grep to confirm.

### Dependencies
- [[RIT-I-0020]] (the `standard→refine` rename) and [[RIT-I-0023]] flow-layer vocabulary.

### Risk Considerations
- Do NOT delete the deprecated alias skill or change slugs; only fix routing/vocabulary.
- Keep any legitimately-deprecated back-compat note explicit.

### Recommended Agent
`sonnet + medium` — focused doc edits across a known set of files with a grep-verifiable outcome.

## Verification Steps **[REQUIRED]**

- [ ] `grep -rn "standard\|update-best-practices" plugins/resume-intelligence/skills` reviewed; only
      intentional back-compat mentions remain.
- [ ] `uv run --no-sync pytest plugins/resume-intelligence/tests/`

## Status Updates **[REQUIRED]**

*To be added during implementation.*
