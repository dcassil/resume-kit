---
id: record-attribution-and-import
level: task
title: "Record attribution and import-boundary checks"
short_code: "RIT-T-0015"
created_at: 2026-08-04T00:40:00+00:00
updated_at: 2026-08-04T01:06:18.824313+00:00
parent: RIT-I-0003
blocked_by: [RIT-T-0010, RIT-T-0011, RIT-T-0012, RIT-T-0014]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0003
---

# Record attribution and import-boundary checks

## Parent Initiative

[[RIT-I-0003]] — Phase 2 — Document Extraction & Parsing

## Objective

Close the compliance and architecture loop after all port/adapt tasks land, without forcing otherwise parallel implementation tasks to share `references/attribution.md`.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `references/attribution.md` has concrete ported-file rows for document-parser code, prompt, and test files derived from upstream material, each referencing upstream path(s) and SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.
- [ ] Attribution rows cover MarkItDown extraction, date restoration, JSON helpers, prompt constants, structured parse adapter, and characterization tests that copied upstream behavior.
- [ ] A package test enforces no `app.*` imports and no concrete LLM provider imports inside `packages/document-parser/src/resume_kit_document_parser`.
- [ ] All ported/adapted source files contain the required modified-source marker comment block from `references/attribution.md`.

## Implementation Notes

Recommended Agent: sonnet + medium
Append a new section such as `Ported Files — packages/document-parser (RIT-I-0003)` below the existing schemas section in `references/attribution.md`, following the table format already used there. Use upstream paths relative to `apps/backend/`: `app/services/parser.py:35-116`, `app/services/parser.py:119-141`, `app/services/parser.py:144-179`, `app/llm.py:218-348`, `app/llm.py:1090-1181`, and `app/prompts/templates.py:20-94,162-186`. Add an architecture/import test that scans parser package source for forbidden strings/imports: `from app.`, `import app.`, `litellm`, and concrete provider package names. This task intentionally owns the ledger file so implementation tasks remain file-disjoint.

### Files this task may write
- `references/attribution.md`
- `packages/document-parser/tests/test_import_boundaries.py`

### Dependencies
- [[RIT-T-0010]] — Port deterministic date restoration
- [[RIT-T-0011]] — Port MarkItDown text extraction
- [[RIT-T-0012]] — Extract JSON helpers and parse prompts
- [[RIT-T-0014]] — Adapt structured resume parsing behind provider protocol

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git (no add/commit/branch/push). You MAY run
package-local verification for your own package only (e.g. `uv run pytest packages/document-parser`,
`uv run mypy packages/document-parser`); do NOT run repo-wide verification that could race a
concurrent agent. `uv` (0.12.x) and Python 3.12 are installed. Donor code is read-only at
`./upstream/apps/backend/app/`. Depend inward on `resume_kit_core` + `resume_kit_schemas` ONLY;
never import `app.*` or a concrete LLM provider. Assume zero prior context beyond this document.

## Verification Steps

1. uv run pytest packages/document-parser/tests/test_import_boundaries.py
2. uv run ruff check references packages/document-parser/tests/test_import_boundaries.py
3. uv run mypy packages/core packages/schemas packages/document-parser
4. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser && uv run pytest

## Status Updates

*To be added during implementation*