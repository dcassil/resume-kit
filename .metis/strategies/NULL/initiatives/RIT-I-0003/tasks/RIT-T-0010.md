---
id: port-deterministic-date-restoration
level: task
title: "Port deterministic date restoration"
short_code: "RIT-T-0010"
created_at: 2026-08-04T00:40:00+00:00
updated_at: 2026-08-04T01:06:18.593366+00:00
parent: RIT-I-0003
blocked_by: [RIT-T-0009]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0003
---

# Port deterministic date restoration

## Parent Initiative

[[RIT-I-0003]] — Phase 2 — Document Extraction & Parsing

## Objective

Port the pure regex month rehydration helpers from upstream before the structured parser depends on them. This task is file-disjoint from text extraction, JSON helpers, and result models, so it can run in parallel after scaffolding.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] `restore_dates_from_markdown` and the internal date extraction helper match upstream behavior for work experience, education, personal projects, and custom item-list sections.
- [ ] Characterization tests cover upstream examples: full range, present/current range, single date, year-only ignored, already-monthful fields preserved, non-dict entries skipped, and missing sections tolerated.
- [ ] Implementation is pure Python regex logic with no LLM, no MarkItDown, no `app.*` imports, and no network.
- [ ] Source file carries the modified-source marker for `apps/backend/app/services/parser.py` at SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.

## Implementation Notes

Recommended Agent: sonnet + medium
Port upstream `upstream/apps/backend/app/services/parser.py:18-116`: `_MD_DATE_RE`, `_extract_markdown_dates`, and `restore_dates_from_markdown`. Preserve behavior first, including in-place patching semantics unless tests intentionally document a copied-return decision. Strip logging unless useful; if kept, use this package's logger. Do not import upstream `app.services.parser`. Attribution ledger rows are added by the dedicated attribution task, but the source marker must be added here.

### Files this task may write
- `packages/document-parser/src/resume_kit_document_parser/dates.py`
- `packages/document-parser/tests/test_dates.py`

### Dependencies
- [[RIT-T-0009]] — Scaffold document-parser workspace package

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git (no add/commit/branch/push). You MAY run
package-local verification for your own package only (e.g. `uv run pytest packages/document-parser`,
`uv run mypy packages/document-parser`); do NOT run repo-wide verification that could race a
concurrent agent. `uv` (0.12.x) and Python 3.12 are installed. Donor code is read-only at
`./upstream/apps/backend/app/`. Depend inward on `resume_kit_core` + `resume_kit_schemas` ONLY;
never import `app.*` or a concrete LLM provider. Assume zero prior context beyond this document.

## Verification Steps

1. uv run pytest packages/document-parser/tests/test_dates.py
2. uv run ruff check packages/document-parser/src/resume_kit_document_parser/dates.py packages/document-parser/tests/test_dates.py
3. uv run mypy packages/core packages/schemas packages/document-parser
4. uv run ruff check packages tests && uv run mypy packages/core packages/schemas packages/document-parser && uv run pytest

## Status Updates

*To be added during implementation*