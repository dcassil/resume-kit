---
id: e2e-integration-test-85-8-75-8
level: task
title: "E2E integration test (85.8->75.8 regression fixture) + README reconcile + version bump"
short_code: "RIT-T-0111"
created_at: 2026-08-05T17:12:00+00:00
updated_at: 2026-08-06T23:34:45.568157+00:00
parent: RIT-I-0017
blocked_by: [RIT-T-0107, RIT-T-0108, RIT-T-0109, RIT-T-0110]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0017
---

# E2E integration test + README reconcile + version bump

## Parent Initiative

[[RIT-I-0017]]

## Objective **[REQUIRED]**

Close the initiative: an end-to-end integration test proving the ScoreDoc split works and the 85.8->75.8
regression is fixed, reconcile README/docs to the new scoring model, and bump the package version.

## Context

Each prior initiative closes with an E2E + README + version task (e.g. [[RIT-T-0094]], RIT-T-0101). This
task validates the whole projection->rescore->report loop against a real fixture and pins the corrected
regression score so it can't silently regress again.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria **[REQUIRED]**

- [x] An E2E test over a real resume/job fixture asserts: (a) the categorized-skills fixture that produced
      85.8->75.8 now scores at the corrected value (pinned 87.7, and placement equals the canonical
      technicalSkills fixture — proving no placement penalty), (b) `check-structure` and
      `check-job-match` produce coherent, deterministic numbers off the shared ScoreDoc, (c) the ATS-view
      report is identical across facade and CLI (parity; full 4-way parity already covered by
      test_surface_parity.py), (d) export output is byte-for-byte deterministic (guards the additive invariant).
- [x] A grep-based guard/test asserts the matching placement path (`_high_value_text`) reads ScoreDoc and
      no BuildDoc field-name literals; the ats composite path (`engine.py`) is documented as intentionally
      excluded per RIT-T-0108's reduced scope.
- [x] README / plugin docs reconcile to describe BuildDoc vs. ScoreDoc, the deterministic projection, and
      the new ATS-view report; Release Note added (Package 0.8.0 / plugin 1.0.0 — RIT-I-0017).
- [x] Package version bumped 0.7.0 → 0.8.0; uv.lock updated (uv lock --check passes).
- [x] Full test suite green (3451 passed, 1 skipped).

## Test Cases **[CONDITIONAL: Testing Task]**

### Test Case 1: Regression pinned
- **Test ID**: TC-001
- **Expected Results**: the categorized-skills fixture scores at the corrected value, not 75.8.

### Test Case 2: Export unaffected
- **Test ID**: TC-002
- **Expected Results**: rendered PDF/DOCX for a fixture is unchanged vs. pre-initiative baseline.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
Reuse existing E2E harnesses and fixtures; add the categorized-skills fixture. Follow the prior
initiative's close-out task for README/version/lockfile steps.

### Dependencies
Blocked by [[RIT-T-0107]], [[RIT-T-0108]], [[RIT-T-0109]], [[RIT-T-0110]]. Closes the initiative.

### Risk Considerations
Medium — integration surface; the export-unchanged assertion is the key guard on the additive promise.

### Recommended Agent
**opus + medium** — cross-cutting integration + docs/release reconciliation.

## Status Updates **[REQUIRED]**

- 2026-08-05: Created as part of RIT-I-0017 decomposition.
- 2026-08-06: Completed. Added `tests/integration/test_scoredoc_regression_integration.py` (5 tests: regression
  pin @ 87.7, structure/match coherence, ats-view facade≡CLI parity, export determinism, and the grep guard).
  **Equivalence finding:** the two composites are NOT byte-equal (A=88.7 vs B=87.7); the divergence is confined
  entirely to `ats_contribution` (76.7 vs 71.7), which by RIT-T-0108's honored reduced scope still reads BuildDoc
  `technicalSkills`. The regression dimension (`evidence_placement`) is now equal (100.0 == 100.0) — that IS the
  85.8→75.8 fix. Adjusted the assertion accordingly and pinned B's composite. README + plugin README reconciled;
  version 0.7.0→0.8.0; uv.lock refreshed. ruff+mypy clean; full suite 3451 passed / 1 skipped.
- 2026-08-06: Started. **Grep-guard scope reconciled to reality:** RIT-T-0108 was reduced (codex-reviewed) and
  explicitly amended the "zero BuildDoc reads in scoring" line — the ATS composite path (`engine.py`) intentionally
  still reads BuildDoc fields (`technicalSkills`/`workExperience`/`customSections`); only the **matching placement
  path** (`match.py::_high_value_text`) was repointed to ScoreDoc (the real 85.8→75.8 fix, RIT-T-0107). So the
  grep guard is scoped to the matching placement path (must not read raw BuildDoc field names), with the ats path
  documented as intentionally excluded per the reduced contract.
  **Regression assertion strategy:** rather than the (unavailable) exact user 85.8 fixture, assert equivalence —
  a resume with JD terms in a categorized `stringList` custom section scores the SAME overall as the same terms in
  `additional.technicalSkills` (placement > 0, no penalty), and pin that corrected overall so it can't drift.
  **Version:** 0.7.0 → 0.8.0 (per close-out convention; independent of the still-pending 0.7.0 PyPI upload).