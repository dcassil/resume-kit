---
id: repoint-ats-engine-py-skills
level: task
title: "Repoint ats (engine.py) skills_coverage/section_completeness/check-ats-structure onto ScoreDoc"
short_code: "RIT-T-0108"
created_at: 2026-08-05T17:09:00+00:00
updated_at: 2026-08-05T22:21:53.110540+00:00
parent: RIT-I-0017
blocked_by: [RIT-T-0106]
archived: false

tags:
  - "#task"
  - "#phase/active"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0017
---

# Repoint ats engine onto ScoreDoc

## Parent Initiative

[[RIT-I-0017]]

## Objective **[REQUIRED]**

Rewire `packages/ats/engine.py` scoring paths — `_compute_skills_coverage`, `_compute_section_completeness`,
and `check_ats_structure` — to read ScoreDoc sections/entities/zoned index instead of raw BuildDoc fields.

## Context

`engine.py` reads `additional.technicalSkills` (`:215`), checks `workExperience` presence (`:256`,
`:386`), and walks the whole dict (`_extract_all_text`, `:93`). ScoreDoc gives it canonical sections and
extracted entities so structure/coverage reflect what an ATS would segment, and share the substrate with
matching.

## Acceptance Criteria

## Acceptance Criteria **[REQUIRED]**

- [ ] `_compute_skills_coverage`, `_compute_section_completeness`, and `check_ats_structure` read from
      ScoreDoc; no literal `additional`/`technicalSkills`/`workExperience` reads remain in the ats scoring
      path (grep-clean).
- [ ] `check-ats-structure` (parse-safety) uses ScoreDoc detected sections + entities (e.g. presence of
      name/contact, detected Experience/Education/Skills, computed YoE availability).
- [ ] Recommendation generation continues to work, sourced from ScoreDoc-derived signals.
- [ ] The two scores keep their identity: `check-ats-structure` structural, `check-job-match` content;
      both now read the shared ScoreDoc.
- [ ] Unit tests: section-completeness and skills-coverage over ScoreDoc fixtures; structure check flags a
      missing canonical section.

## Test Cases **[CONDITIONAL: Testing Task]**

### Test Case 1: Missing-section detection
- **Test ID**: TC-001
- **Expected Results**: a ScoreDoc lacking an Experience section lowers section-completeness and yields the
  expected recommendation.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
Thread the projection/`ScoreDoc` into `compute_ats_score` and `check_ats_structure`. Preserve public
result schemas; only the internal source of truth changes.

### Dependencies
Blocked by [[RIT-T-0106]]. Parallel with [[RIT-T-0107]]. Feeds [[RIT-T-0111]].

### Risk Considerations
Medium — structure/coverage numbers may shift; covered by 0111. Keep recommendation text stable where behavior is unchanged.

### Recommended Agent
**opus + medium** — focused single-module rewire following the established pattern.

## Status Updates **[REQUIRED]**

- 2026-08-05: Created as part of RIT-I-0017 decomposition.
- 2026-08-05 — PAUSED pending review. Analysis of the current `_compute_skills_coverage` shows it ALREADY harvests categorized skills via its `_extract_all_text` free-text fallback, so the ScoreDoc repoint of the composite-scoring path is mostly architectural (decouple from BuildDoc field names) with marginal score change but HIGH characterization-test churn risk (59 ats tests) and requires threading a `reference_date` through `compute_ats_score` and its callers. Per session guidance, deferring the scoring-function repoint to a reviewed pass with the real 85.8->75.8 fixture. The genuine zone-weighting FIX belongs to RIT-T-0107 (matching `_high_value_text`). ScoreDoc + projection (RIT-T-0106) are DONE and available.
