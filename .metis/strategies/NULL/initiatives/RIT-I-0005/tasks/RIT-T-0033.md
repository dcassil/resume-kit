---
id: port-structured-diff-and-verifier
level: task
title: "Port Structured Diff And Verifier"
short_code: "RIT-T-0033"
created_at: 2026-08-04T04:10:00+00:00
updated_at: 2026-08-04T04:35:17.252761+00:00
parent: RIT-I-0005
blocked_by: [RIT-T-0027]
archived: false

tags:
  - "#task"
  - "#phase/completed"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0005
---

# Port Structured Diff And Verifier

## Parent Initiative

[[RIT-I-0005]] — Phase 4 — Controlled Alignment

## Objective

Port the deterministic content-diff and structural verifier from upstream `upstream/apps/backend/app/services/improver.py`. Grounding symbols are `calculate_resume_diff` at `improver.py:1224`, `DiffConfidence`, helper logic for case-insensitive order-ignored skills/certs/languages/awards, description/summary/experience/education/project comparisons, and `verify_diff_result` at `improver.py:430` with `_count_description_words` and `_METRIC_RE`. Use upstream tests `upstream/apps/backend/tests/unit/test_resume_diff.py` and `upstream/apps/backend/tests/unit/test_verify_diffs.py` as characterization sources.

## Acceptance Criteria

## Acceptance Criteria

## Acceptance Criteria

- [ ] Characterization tests first reproduce upstream `calculate_resume_diff` behavior for summary added/removed/modified, skill add/remove case-insensitive, skill order ignored, certification add/remove, language/award changes, description changes, malformed list handling, and experience/education/project entry changes.
- [ ] Characterization tests first reproduce upstream `verify_diff_result` warnings for no applied changes, dropped work/education/projects, identity field changes, word-count explosion, invented percentages, invented dollar amounts, preserved metrics, and multiple warnings.
- [ ] `calculate_resume_diff` returns existing schema `ResumeDiffSummary` and `Diff` values from `resume_kit_schemas.change`.
- [ ] `verify_diff_result` returns structured warnings or strings consistently with the rest of alignment, and is usable by `align_resume` without provider dependencies.
- [ ] Metrics are treated conservatively: newly introduced `%`, `x`, or `$` metrics not present in the original change text are warned.
- [ ] The implementation is deterministic and imports no `app.*`, LLM provider, policy, or evidence modules unless strictly necessary.
- [ ] Every ported source file has the modified-source marker required by `references/attribution.md` using SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.

## Implementation Notes

Use `packages/alignment/src/resume_kit_alignment/diff.py` for `calculate_resume_diff` and `packages/alignment/src/resume_kit_alignment/verify.py` for `verify_diff_result`. Keep this task file-disjoint from `apply.py`; do not edit orchestration or exports. Where upstream returned plain strings, either keep strings internally or map to `resume_kit_schemas.Warning` in a small local helper, but avoid changing schema files.

### Files this task may write
- `packages/alignment/src/resume_kit_alignment/diff.py`
- `packages/alignment/src/resume_kit_alignment/verify.py`
- `packages/alignment/tests/test_resume_diff_characterization.py`
- `packages/alignment/tests/test_verify_diff_result_characterization.py`

### Dependencies
- [[RIT-T-0027]] — Add Phase 4 Result Schemas

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and sibling Phase-4 packages only if noted) ONLY; never
import `app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this
task IS the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv run mypy packages/alignment
2. uv run pytest packages/alignment/tests/test_resume_diff_characterization.py packages/alignment/tests/test_verify_diff_result_characterization.py

## Status Updates

*To be added during implementation*