---
id: port-evidence-predicates-and-fabrication-checks
level: task
title: "Port Evidence Predicates And Fabrication Checks"
short_code: "RIT-T-0030"
created_at: 2026-08-04T04:10:00+00:00
updated_at: 2026-08-04T04:10:00+00:00
parent: RIT-I-0005
blocked_by: [RIT-T-0027]
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: NULL
initiative_id: RIT-I-0005
---

# Port Evidence Predicates And Fabrication Checks

## Parent Initiative

[[RIT-I-0005]] — Phase 4 — Controlled Alignment

## Objective

Build the deterministic evidence seed from upstream structural truth code. Port structural eval scorers from `upstream/apps/backend/tests/evals/scorers.py`: `sections_preserved` at `scorers.py:100`, `no_fabricated_employers` at `scorers.py:117`, and `personal_info_unchanged` at `scorers.py:138`; `jd_keywords_present` and `is_valid_resume` already landed in `packages/matching/src/resume_kit_matching/predicates.py` and should not be duplicated unless evidence needs local truth wrappers. Port fabrication detection/removal from `upstream/apps/backend/app/services/refiner.py`: `validate_master_alignment` at `refiner.py:290` and `fix_alignment_violations` at `refiner.py:591`.

## Acceptance Criteria

- [ ] Characterization tests first reproduce upstream scorer behavior for preserved sections, fabricated employers, and unchanged personal info.
- [ ] Characterization tests first reproduce upstream `validate_master_alignment` and `fix_alignment_violations` behavior for fabricated skills, allowed JD-added skills, skill variants, fabricated certifications, fabricated companies, confidence decrease, and critical-only removal.
- [ ] `predicates.py` exposes reusable deterministic truth predicates over `ResumeDocument` or plain dict input.
- [ ] `fabrication.py` exposes `validate_master_alignment` and `fix_alignment_violations` returning existing schema `AlignmentReport` / `AlignmentViolation` from `resume_kit_schemas.analysis`.
- [ ] Allowed new skills are normalized consistently with upstream, including punctuated skills.
- [ ] No unsupported company, certification, or critical fabricated skill passes as aligned.
- [ ] Every ported source file has the modified-source marker required by `references/attribution.md` using SHA `116f9cc3b00e1ac91734a6c2679bf41ea64a0edc`.

## Implementation Notes

Use `packages/evidence/src/resume_kit_evidence/predicates.py` and `fabrication.py`. Reuse schema models from `resume_kit_schemas.analysis` instead of creating evidence-local violation classes. Keep deterministic text extraction private. Avoid imports from `resume_kit_alignment` to preserve the dependency graph. Do not edit `resume_kit_evidence/__init__.py`.

### Files this task may write
- `packages/evidence/src/resume_kit_evidence/predicates.py`
- `packages/evidence/src/resume_kit_evidence/fabrication.py`
- `packages/evidence/tests/test_predicates_characterization.py`
- `packages/evidence/tests/test_fabrication_characterization.py`

### Dependencies
- [[RIT-T-0027]] — Add Phase 4 Result Schemas

### Constraint (teamwork orchestration)
Implementing agent WRITES FILES ONLY. Do NOT run git. You MAY run package-local verification for
your own package(s) only; do NOT run repo-wide verification that could race a concurrent agent.
`uv` (0.12.x) + Python 3.12 installed; run `uv sync --all-packages` if you need the new package
importable. Donor code read-only at `./upstream/apps/backend/app/`. Depend inward on
`resume_kit_core` + `resume_kit_schemas` (and sibling Phase-4 packages only if noted) ONLY; never
import `app.*` or a concrete LLM provider. Do NOT edit package `__init__.py` exports unless this
task IS the exports task. Assume zero prior context beyond this document.

## Verification Steps

1. uv run mypy packages/evidence
2. uv run pytest packages/evidence/tests/test_predicates_characterization.py packages/evidence/tests/test_fabrication_characterization.py

## Status Updates

*To be added during implementation*
